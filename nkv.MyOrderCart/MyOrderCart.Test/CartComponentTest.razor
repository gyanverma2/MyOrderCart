@code
{
	[Fact]
	public void CartComponent_EmptyCart()
	{
		using var ctx = new TestContext();
		ctx.Services.AddTransient<IDialogService,DialogService>();
		ctx.Services.AddTransient<IDummyRequestAPI, DummyRequestAPI>();
		var cut = ctx.Render(@<CartComponent />);
		cut.Find("h5").TextContent.MarkupMatches("Your Cart: Is Empty");
	}
}
